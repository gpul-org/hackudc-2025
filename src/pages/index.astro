---
import {
  faInfo,
  faLaptopCode,
  faSuitcase,
} from '@fortawesome/free-solid-svg-icons'
import { FontAwesomeIcon } from '@fortawesome/react-fontawesome'
import Logo from '../components/Logo.astro'
import HeaderLayout from '../layouts/HeaderLayout.astro'

const scrollOptions = [
  {
    icon: faInfo,
    title: 'Info. general',
    subtitle: 'Fechas, ubicación, etc.',
  },
  {
    icon: faLaptopCode,
    title: 'Participar',
    subtitle: 'Fechas, ubicación, etc.',
  },
  {
    icon: faSuitcase,
    title: 'Patrocinar',
    subtitle: 'Fechas, ubicación, etc.',
  },
]
---

<HeaderLayout title="HackUDC 2025">
  <div class="mx-auto mt-8 w-[300px] sm:w-[400px] md:w-[500px]">
    <Logo />
  </div>
  <p class="mt-12 text-center text-2xl sm:text-3xl md:text-5xl">
    La tercera edición del mayor <br />
    <span
      class="inline-block bg-gradient-to-br from-green-700 via-green-500 to-green-700 bg-clip-text text-transparent"
      >hackathon estudiantil</span
    > de Galicia
  </p>
  <div class="mt-14 grid grid-cols-1 gap-4 px-2 sm:grid-cols-3">
    {
      scrollOptions.map((o) => {
        return (
          <div class="bg-animated flex h-36 flex-col justify-evenly rounded p-0.5 duration-150 hover:scale-[1.03]">
            <div class="flex h-full flex-col justify-evenly rounded bg-[#242424] px-8 py-2 duration-150">
              <div class="px-1">
                <FontAwesomeIcon icon={o.icon} className="h-10" />
              </div>
              <div>
                <h2 class="text-xl">{o.title}</h2>
                <p class="text-sm font-light">{o.subtitle}</p>
              </div>
            </div>
          </div>
        )
      })
    }
  </div>
</HeaderLayout>

<style>
  .bg-animated:hover {
    animation: gradient 1s ease;
    animation-iteration-count: 1;
    animation-fill-mode: forwards;
  }

  .bg-animated {
    background: linear-gradient(-45deg, #22c55e, #15803d 50%, #242424 50% 100%);
    background-size: 200% 200%;
  }
  @keyframes gradient {
    0% {
      background-position: 0% 0%;
    }
    100% {
      background-position: 100% 100%;
    }
  }
</style>
